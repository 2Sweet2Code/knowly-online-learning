# Supabase
VITE_SUPABASE_URL=your-supabase-project-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Generate your types by running:
# 1. Install Supabase CLI: npm install -g supabase
# 2. Run: npx supabase gen types typescript --project-id your-project-ref > src/types/database.types.ts
# Replace 'your-project-ref' with your Supabase project reference (found in project settings)

# Note: Never commit your actual .env file to version control!
